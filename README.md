# Dwm-omar

Installation

  ./setup-arch.sh
  
  cd dwm/
  sudo make clean install
  
  cd ../slstatus/
  sudo make clean install
  
  cd ../dmenu/
  sudo make clean install

  cd ../config/
  cp -r * ~/.config/

  sudo cp -r /usr/share/ /usr/
